#' convert_int_to_position
#'
#' In a sequence of incremented binary numbers with integer values starting from 0 to n,
#' the index position of the binary number with inteteger value 0 is 1.
convert_int_to_position <- function(i){
  return(i + 1);
}
